name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: "release"
  cancel-in-progress: false

jobs:
  build-android:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Get version from pubspec.yaml
        id: get_version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | awk '{print $2}')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build Android APK
        run: flutter build apk --release

      - name: Build Android App Bundle
        run: flutter build appbundle --release

      - name: Rename APK
        run: |
          VERSION=${{ steps.get_version.outputs.version }}
          cp build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/nmail-${VERSION}-android.apk

      - name: Rename AAB
        run: |
          VERSION=${{ steps.get_version.outputs.version }}
          cp build/app/outputs/bundle/release/app-release.aab build/app/outputs/bundle/release/nmail-${VERSION}-android.aab

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: nmail-apk
          path: build/app/outputs/flutter-apk/nmail-${{ steps.get_version.outputs.version }}-android.apk

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: nmail-aab
          path: build/app/outputs/bundle/release/nmail-${{ steps.get_version.outputs.version }}-android.aab

  build-linux:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev libstdc++-12-dev libsecret-1-dev libfuse2
          sudo wget -O /usr/local/bin/appimagetool https://github.com/AppImage/appimagetool/releases/download/1.9.1/appimagetool-x86_64.AppImage
          sudo chmod +x /usr/local/bin/appimagetool

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Install Fastforge
        run: dart pub global activate fastforge

      - name: Get version from pubspec.yaml
        id: get_version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | awk '{print $2}')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build Linux packages
        run: fastforge package --platform linux --targets deb,appimage

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: nmail-deb
          path: dist/**/*.deb

      - name: Upload AppImage artifact
        uses: actions/upload-artifact@v4
        with:
          name: nmail-appimage
          path: dist/**/*.AppImage

  create-release:
    runs-on: ubuntu-latest
    needs: [build-android, build-linux]
    steps:
      - uses: actions/checkout@v4

      - name: Download APK
        uses: actions/download-artifact@v4
        with:
          name: nmail-apk
          path: release-artifacts

      - name: Download AAB
        uses: actions/download-artifact@v4
        with:
          name: nmail-aab
          path: release-artifacts

      - name: Download .deb
        uses: actions/download-artifact@v4
        with:
          name: nmail-deb
          path: release-artifacts

      - name: Download AppImage
        uses: actions/download-artifact@v4
        with:
          name: nmail-appimage
          path: release-artifacts

      - name: List artifacts
        run: ls -la release-artifacts/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            release-artifacts/*
          generate_release_notes: true
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
